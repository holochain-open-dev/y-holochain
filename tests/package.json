{
  "name": "tests",
  "private": true,
  "scripts": {
    "build": "cd demo && npm install && npm run build:zomes && hc app pack workdir --recursive",
    "test": "npm run build && vitest run",
    "start": "npm run build && cd demo && npm run start"
  },
  "dependencies": {
    "@msgpack/msgpack": "^2.7.0",
    "@holochain/client": "^0.15.0",
    "typescript": "^4.9.4",
    "vitest": "^0.28.4"
  },
  "type": "module"
}
