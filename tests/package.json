{
  "name": "tests",
  "private": true,
  "scripts": {
    "build": "cd demo && npm install && npm run build:happ",
    "test": "npm run build && vitest run",
    "start": "npm run build && cd demo && npm run start"
  },
  "dependencies": {
    "@holochain/client": "^0.16.0",
    "@msgpack/msgpack": "^2.7.0",
    "typescript": "^4.9.4",
    "vitest": "^0.28.4"
  },
  "type": "module"
}
