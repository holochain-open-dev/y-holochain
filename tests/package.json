{
  "name": "tests",
  "private": true,
  "scripts": {
    "build": "cd demo && npm run build:zomes && hc app pack workdir --recursive",
    "test": "npm run build && vitest run"
  },
  "dependencies": {
    "@msgpack/msgpack": "^2.7.0",
    "@holochain/client": "^0.15.0",
    "@holochain/tryorama": "^0.15.0-rc.1",
    "typescript": "^4.9.4",
    "vitest": "^0.28.4"
  },
  "type": "module"
}
